# projects

Workspace for competition-specific experiments that consume `my_library`.

## Structure

- `my_projects/` – Personal competition pipelines or case studies.
- `others/` – Third-party examples, references, or collaborative workspaces.

Each subproject should define its own virtual environment or reference the Docker workflows described in the root `README.md`. Keep sensitive competition data outside the repository and mount it at runtime.
