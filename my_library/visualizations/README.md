# visualizations

Plotting helpers for exploratory data analysis and model diagnostics.

## Modules

- `distribution_plot.py` – Utilities for visualizing feature distributions, comparisons between train/test splits, and detecting drift.

## Usage

Import the needed function and pass pandas DataFrames or Series as required. Keep plots generation inside notebooks or reporting scripts so the core package remains headless.
